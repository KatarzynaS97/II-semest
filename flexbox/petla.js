
function randTab()
{
    //generuj tablice
    const array = [];
    for(let i=0; i<3; i++){
    const row = [];
    for(let j = 0; j<3; j++)
    {
        row.push(getRandomInt(1,100));//zakres od 1 do100
    }
    array.push(row);
}
}

function minmax(tab)
[
    //min i max 
]

function licz()
{

let tab1 = array[0][0];
let tab2 = array[0][0];

for(let i = 0;  i<3; i++)
    for(let j=0; j<3; j++)
{
    if(array[i][j]<minValue){
        minValue= array[i][j];
    }
    if(array[i][j]< maxValue){
        maxValue=array[i][j];
    }
}

}